fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic[email]==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
python-dotenv==1.0.1
httpx==0.28.1
# PostgreSQL driver — used only when DATABASE_URL points at Postgres (e.g.
# Supabase in production). SQLite (the local default) needs no driver.
psycopg[binary]==3.2.3
